# Default configuration. Values can be overridden via -D system properties,
# e.g. mvn test -DbaseUrl=... -Dbrowser=firefox -Dheadless=false
baseUrl=https://ecommerce-playground.lambdatest.io
browser=chromium
headless=true
defaultTimeout=45000
slowMo=0